import { Container } from 'react-bootstrap';
import styles from './Home.module.scss';

const Home = () => {
    return (
        <Container>
            <h1>HOME PAGE</h1>
        </Container>
    );
};

export default Home;
